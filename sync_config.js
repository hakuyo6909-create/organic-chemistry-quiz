/* ============================================================
   有機化学クイズ — ログ集約（クラウド送信）の設定
   ------------------------------------------------------------
   生徒の操作ログを、先生の Google スプレッドシートに自動集約する
   ための設定ファイルです。

   ★使い方（先生）★
   1. 「ログ集約_設定手順.md」の手順で Google Apps Script を用意し、
      発行された「ウェブアプリの URL」を取得する。
   2. 下の url: "" のダブルクオートの中に、その URL を貼り付ける。
   3. secret はスパム防止の合言葉。Apps Script 側と同じ文字列にする
      （空でも動くが、設定推奨）。
   4. ファイルを保存し、GitHub に push（またはUSBに上書き）して配布。

   url が空のままなら送信機能は完全に無効（オフライン動作のまま）です。
   送信されるのは出席番号・操作ログのみで、氏名は含まれません。
   ============================================================ */
(function (global) {
  'use strict';
  global.ORGQUIZ_SYNC = {
    // GAS ウェブアプリの URL（例: https://script.google.com/macros/s/AKfy.../exec）
    url: "",
    // 合言葉（Apps Script の SECRET と一致させる。空可）
    secret: "",
  };
})(window);
